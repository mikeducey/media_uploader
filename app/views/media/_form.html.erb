<%= form_for @media do |f| %>

  <% if @media.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@media.errors.count, "error") %> prohibited this video from being saved:</h2>

      <ul>
      <% @media.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :video %>
    <%= f.file_field :media %><br>

    <%= f.label :description %>
    <%= f.text_field :description %><br>
  </div>
  
  <div>
    <%= f.submit "Upload the file" %>
  </div>
<% end %>
